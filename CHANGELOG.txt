0.4.8
- added ability to delete imported tile groups
- tile name and edit stuff stays fixed at the bottom of the tile panel instead of scrolling with the tiles

0.4.7
- added ability to paint with full tile groups insteaf of just 8x8 tiles
- added ability to set a collision type for a tile, so when you draw with a selected tile, you also draw the selected collision group on top of it at the same time
- top most layer is selected by default, so you can draw right away

0.4.6
- fixed linux permissions build error

0.4.5
- adds initial GB Studio project importing (don't expect stuff to just work, it's a work in progress!)
- removed billion step welcome tour

0.4.4
- collapsing/expanding sidebars no longer resets zoom/pan

0.4.3
- slims down to 2 sidebars, you may need to 'reset layout' in the footer to see the change
- minor ui tweaks to sidebar panels
- double-click header to toggle sidebars
- added edit button to scenes in global map overlay

0.4.2
- added changelog to auto update

0.4.1
- fixed freesound search results

0.4.0
- adds panel backgrounds based on their primary function
- adds ability to reorganize panels by dragging them (saves automatically, reset in footer)
- fixed hud vertical layout compile bug
- fixed magenta flicker between scenes
- fixed tile editor palette limits
- added axis locking to projectiles
- added life time for projectiles
- fixed race level compile error
- splits music and sfx into separate panels
- adds freesound.org sfx search to sfx
- moved text out of sidebar into a flyout menu

0.3.5
- adds tile editor with live preview
- adds scene settings variables, and defaults new scenes to use them (velocity, traction, speed, etc)
- adds 'get actor position' script node
- brings over new script node types from the gbstudio import branch
- adds support for .wav file importing for both bgm and sfx (warning provided for large wav files)

0.3.4
- added 'pass wall' actor type

0.3.3
- fixed windows compile error (hopefully it works for everyone!)
- extended check input script node options
- script nodes auto connect UNTIL you have added a menu or check input node (then you have to manually connect them)
- adds controller support to play test and html export


0.3.2
- fixed movement/facing projectile direction bug 
- fixed sliced actor sprites 
- fixed dialog and hud text colors (again, pesky bug!)
- fixed updater issue on desktop builds 

0.3.1
- added layer support to actor designer
- added layer nudging to actor designer
- when importing tiles, a folder is created for the imported tiles for better organization

0.3.0
- saves zoom/pan to project
- added initial documentation
- electron app now tries to install all build tools on first launch


0.2.9
- added updating system within the app (in the status bar footer)
- fixed large player actor sprites crashing on jump with animation
- added jump animation state to actor designer (is triggered automatically on jump)

0.2.8
- added health pickup item actor
- added player variables
- simplified player actor config
- added grouped tiles for large sprites naming
- fixed bitmap bug on jump landing transition to other animation
- fixed hud text color
- added movement config to pickup items missing them
- fixes scrolling in collision and trigger groups
- save warning in status bar now saves the project when clicked
- new scenes automatically get actor groups added to actors panel for organization
- new actors added will automatically be added to the correct group in the actors panel for organization
- fixed shield pickup


0.2.7
- fixed animation bug in actor designer (thanks Lucas8537!)
- updated actor designer UI a bit
- updated example game project
- added the 'laps to finish' option to the racing scene config in the scenes panel. 
- added 'on finish' script option to racing scene config in the scenes panel
- added rom size status bar 
- adds save tracker to status bar
- adds support for up to 64x64 tiles

0.2.6
- adding a new actor now adds the actor to the middle of the current view, not the middle of the scene
    - so if you're zoomed into an area of a large map, adding an actor adds it where you can see into
- replaced simple tile name only dropdown with a searchable and scrollable tile selector in HUD settings
- fixed canvas clicking when an actor is selected adding another actor to the scene
- adds support for 16x16 and 32x32 tile imports
    - sprite picker and designer updated accordingly
- adds background remover for imported tile sets to the actor designer
- adds a movement range visualization for actors and enemies
- when an actor is selected and has movement enabled, a colored overlay shows the movement range 
    - green overlay for horizontal movement
    - blue overlay for vertical movement
    - orange overlay for bounce movement
    - works for both actors and enemies (checks moveAmount and enemyRange properties)
    - visualization only appears when: 
        - using the actor or spawn tool
        - the actor is selected (activeActorId matches)
        - the actor has isMoving enabled
        - the movement range is greater than 0
- locks down default PLAYER variables
    - a lot of the default actor types and HUD stuff is linked to those variables